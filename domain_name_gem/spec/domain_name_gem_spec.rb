require 'spec_helper'

describe DomainNameGem do
  it 'has a version number' do
    expect(DomainNameGem::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
